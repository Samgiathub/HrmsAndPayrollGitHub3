

CREATE PROCEDURE [dbo].[P0050_GENERAL_LATEMARK_SLAB_SCENARIO4]
	@TRANS_ID NUMERIC(18,0),
    @CMP_ID NUMERIC(18,0),
    @FROM_MIN NUMERIC(18,0),
    @TO_MIN NUMERIC(18,0),
    @FROM_COUNT NUMERIC(18,0),
	@TO_COUNT NUMERIC(18,0),
	@DEDUCTION NUMERIC(18,2),
	@GEN_ID NUMERIC(18,0),
	@MODIFY_BY NUMERIC(18,0),
	@IP_ADDRESS VARCHAR(20)
AS

        SET NOCOUNT ON 
		SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED
		SET ARITHABORT ON

BEGIN
	-- SET NOCOUNT ON added to prevent extra result sets from
	-- interfering with SELECT statements.
	
	Begin
		INSERT INTO T0050_GENERAL_LATEMARK_SLAB_SCENARIO4 Values(@CMP_ID,@FROM_MIN,@TO_MIN,@FROM_COUNT,@TO_COUNT,@DEDUCTION,@GEN_ID,@MODIFY_BY,GetDate(),@IP_ADDRESS)
	End
END
