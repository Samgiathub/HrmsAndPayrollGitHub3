CREATE TABLE [dbo].[T0040_HRMS_R_PROCESS_MASTER] (
    [Process_ID]   NUMERIC (18)    NOT NULL,
    [Process_Name] VARCHAR (50)    NOT NULL,
    [Process_Desc] NVARCHAR (1000) NULL,
    [Cmp_id]       NUMERIC (18)    NOT NULL,
    CONSTRAINT [PK_T0040_HRMS_R_PROCESS_MASTER] PRIMARY KEY CLUSTERED ([Process_ID] ASC) WITH (FILLFACTOR = 80)
);

