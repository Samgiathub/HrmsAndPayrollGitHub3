CREATE TABLE [dbo].[T0110_ATTRIBUTE_WEIGHTAGE] (
    [Row_Id]    NUMERIC (18)    NOT NULL,
    [Cmp_Id]    NUMERIC (18)    NOT NULL,
    [Attr_Id]   NUMERIC (18)    NOT NULL,
    [Tran_ID]   NUMERIC (18)    NOT NULL,
    [Weightage] NUMERIC (18, 2) NOT NULL,
    CONSTRAINT [PK_T0110_ATTRIBUTE_WEIGHTAGE] PRIMARY KEY CLUSTERED ([Row_Id] ASC),
    CONSTRAINT [FK_T0110_ATTRIBUTE_WEIGHTAGE_T0110_ATTRIBUTE_WEIGHTAGE] FOREIGN KEY ([Tran_ID]) REFERENCES [dbo].[T0100_EMP_SKILL_ATTR_ASSIGN] ([Tran_ID])
);

